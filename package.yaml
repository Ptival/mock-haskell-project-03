dependencies:
  - base

executable:
  dependencies:
    - project
    - time
  source-dirs: exec
  main: Main.hs

library:
  dependencies:
    - containers
  source-dirs: lib
  exposed-modules:
    - ExposedModule
  other-modules:
    - OtherModule

name: project

version: 0.0.1
